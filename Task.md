# JavaScript Exercise Questions

## 1. Variables

1. Declare a variable named `schoolName` and assign your school name to it.
2. Create a variable `age` and store  age in it.
3. Declare a variable using `let` called `score` and assign it the value 85.
4. Declare a constant variable `PI` and assign it the value 3.142.
5. Create two variables `firstName` and `lastName` and assign values to them.

 
7. Create a variable without assigning a value. Print it to the console.  
 

---

## 2. Data Types

1. Create a string variable called `studentName`.
2. Create a number variable called `totalMarks`.
3. Create a boolean variable called `isPresent`.
4. Create an array called `subjects` with 3 subjects.
5. Create an object called `student` with properties: `name`, `age`, and `class`.

 

---

## 3. Operators

### Arithmetic Operators
1. Add two numbers: 15 and 20.
2. Subtract 50 from 100.
3. Multiply 12 by 4.
4. Divide 100 by 5.
5. Find the remainder when 17 is divided by 3.

### Assignment Operators
6. Declare `x = 10`, then add 5 to it using `+=`.
7. Multiply a variable `y = 6` by 2 using `*=`.

### Comparison Operators
8. Compare 10 and `"10"` using `==`.
9. Compare 10 and `"10"` using `===`.
10. Check if 25 is greater than 18.

### Logical Operators
11. Check if 10 is greater than 5 AND 20 is less than 30.
12. Check if 15 is less than 10 OR 5 is equal to 5.
13. Use the NOT operator on `true`.

---

## 4. Output

1. Print `"Hello World"` to the console.
2. Print your name and age in one sentence using concatenation.
3. Use template literals to print:  
   `My name is John and I am 20 years old.`

4. Display `"Welcome to JavaScript"` using `alert()`.
5. Use `document.write()` to print `"Learning JavaScript is fun!"`.
6. Select an HTML element with id `demo` and display `"JavaScript Output Example"` inside it.

### Practical Exercise
7. Create a small program that:
   - Declares two numbers
   - Adds them
   - Displays the result in the console
8. Create a program that takes a student's name and score and prints:
   `"Student John scored 85 marks."`
   ` "student " + name +"scored" + scorevalue + " marks"`

